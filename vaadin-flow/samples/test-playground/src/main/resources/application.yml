logging:
   level:
      org.springframework: WARN

spring:
   profiles:
      active: dev
   main:
      banner-mode: 'off'
   devtools:
      restart:
        poll-interval: "2s"
        quiet-period: "1s"

server:
   port: 8080
   servlet:
      context-path: '/linkki-sample-test-playground-vaadin-flow'
