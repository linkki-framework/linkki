logging:
   level:
      org.springframework: WARN

spring:
   profiles:
      active: dev
   main:
      banner-mode: 'off'

server:
   port: 8080
   servlet:
      context-path: '/linkki-getting-started-vaadin14'
